create database login;
use login;
create TABLE usuario (
 id_usuario INT AUTO_INCREMENT PRIMARY key,
 nome varchar(45),
 Cpf varchar(45),
 email VARCHAR(45),
 senha varchar(45),
 telefone VARCHAR(45)
) ;

